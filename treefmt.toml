[formatter.alejandra]
command = "alejandra"
includes = ["*.nix"]

[formatter.dprint]
command = "dprint"
options = ["fmt"]
includes = ["*.json", "*.toml", "*.md"]

[formatter.dhall]
command = "dhall"
options = ["format"]
includes = ["*.dhall"]
